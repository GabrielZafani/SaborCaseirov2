<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Home - Sabor Caseiro</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<style>
.cards-grid {
  display: grid;
  grid-template-columns: repeat(3, 30rem); /* padrão desktop */
  justify-content: center;
  gap: 2rem;
}

.cards-grid .card {
  width: 30rem;
}

/* Tablet (máx. 992px) */
@media (max-width: 992px) {
  .cards-grid {
    grid-template-columns: repeat(2, 1fr); /* 2 por linha */
  }
  .cards-grid .card {
    width: 100%; /* ocupa toda a coluna */
  }
}

/* Celular (máx. 576px) */
@media (max-width: 576px) {
  .cards-grid {
    grid-template-columns: 1fr; /* só 1 por linha */
  }
  .cards-grid .card {
    width: 100%; /* ocupa toda a largura disponível */
  }
}

</style>





    <div id="carouselExample" class="carousel slide mb-5">
      <div class="carousel-inner">
        <div class="carousel-item active">
          <img src="/img/banner1.png" class="d-block w-100" alt="Banner 1" style="height: 500px; ">
        </div>
        <div class="carousel-item">
          <img src="/img/banner2.png" class="d-block w-100" alt="Banner 2" style="height: 500px; ">
        </div>
      </div>
      <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Anterior</span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Próximo</span>
      </button>
    </div>

   <h2 class="text-center mb-4">Bolos em Destaque</h2>
<div class="container mb-4">
  <div class="cards-grid">
      <?php foreach ($produtosDestaque as $p): ?>
          <div class="card">
              <img src="/img/<?= htmlspecialchars($p['imagem'] ?? 'image.gif') ?>" 
                   alt="<?= htmlspecialchars($p['nome']) ?>" 
                   class="card-img-top" style="height: 200px; object-fit: cover;">
              <div class="card-body text-center">
                  <h5 class="card-title"><?= htmlspecialchars($p['nome']) ?></h5>
                  <p class="card-text"><strong>R$ <?= number_format($p['valor'], 2, ',', '.') ?></strong></p>
                  <a href="/produto/<?= $p['id'] ?>" class="btn btn-danger">Encomendar</a>
              </div>
          </div>
      <?php endforeach; ?>
  </div>
</div>




            <?php if (empty($produtosDestaque)): ?>
                <p class="text-center">Nenhum produto em destaque encontrado.</p>
            <?php endif; ?>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

   
</body>
</html>
