<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Home - Sabor Caseiro</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<style>
.cards-grid {
  display: grid;
  grid-template-columns: repeat(3, 30rem); 
  justify-content: center;
  gap: 2rem;
}

.cards-grid .card {
  width: 30rem;
}

/
@media (max-width: 992px) {
  .cards-grid {
    grid-template-columns: repeat(2, 1fr); 
  }
  .cards-grid .card {
    width: 100%; 
  }
}


@media (max-width: 576px) {
  .cards-grid {
    grid-template-columns: 1fr; 
  }
  .cards-grid .card {
    width: 100%; 
  }
}

</style>




<img src="/img/banner1.png" class="d-block w-100" alt="Banner 1" style="height: 800px; padding-top: 10px;">

<h2 class="text-center mb-4" style="padding-top: 50px;">Bolos em Destaque</h2>
<div class="container mb-4">
  <div class="cards-grid">
      <?php foreach ($produtosDestaque as $p): ?>
          <div class="card">
              <img src="/img/<?= htmlspecialchars($p['imagem'] ?? 'image.gif') ?>" 
                   alt="<?= htmlspecialchars($p['nome']) ?>" 
                   class="card-img-top" style="height: 200px; object-fit: cover;">
              <div class="card-body text-center">
                  <h5 class="card-title"><?= htmlspecialchars($p['nome']) ?></h5>
                  <p class="card-text"><strong>R$ <?= number_format($p['valor'], 2, ',', '.') ?></strong></p>
                  <a href="/produto/<?= $p['id'] ?>" class="btn btn-danger">Encomendar</a>
              </div>
          </div>
      <?php endforeach; ?>
  </div>
</div>




            <?php if (empty($produtosDestaque)): ?>
                <p class="text-center">Nenhum produto em destaque encontrado.</p>
            <?php endif; ?>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

   
</body>
</html>
